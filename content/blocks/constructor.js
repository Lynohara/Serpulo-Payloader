// Constructs blocks up to 2x2 in size. Ideal for Payload Mass Drivers with Copper Deconstructors
// maxBlockSize: 2

const lib = require("blib");
const copConstructor = extend(Constructor, "Copper Constructor", {
    size:3,
    health: 600
});
